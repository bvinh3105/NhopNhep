# Same-origin proxy to Overpass API (fixes CORS + adblockers)
/api/overpass  https://overpass-api.de/api/interpreter  200!

# Wikimedia tile fallback
/tiles/wiki/*  https://maps.wikimedia.org/osm-intl/:splat  200!

# SPA fallback (must come last)
/*  /index.html  200
