# SPA fallback (Cloudflare Pages + Netlify both honor this syntax).
# The /api/overpass route is served by functions/api/overpass.js on
# Cloudflare Pages and by netlify.toml -> functions/overpass on Netlify.
/*  /index.html  200
